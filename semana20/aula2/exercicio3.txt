exports.handler = async (event) => {
	const soma = JSON.parse(event.num1) + JSON.parse(event.num2)
	const response = {
		statusCode: 200,
		body: JSON.stringify(`Soma: ${soma}`)
	}
	return response
}
	